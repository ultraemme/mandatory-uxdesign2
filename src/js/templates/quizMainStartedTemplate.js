export default function quizMainStartedTemplate(round) {
  return `<h2 class="quiz__header">Quiz ${round}</h2>
    <form action="">
      
      <button type="submit" class="quiz__button quiz__button-submit">Done!</button>
    </form>`
}